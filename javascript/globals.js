const baseURL = "<update with your host name - e.g. https://viya.yourorg.com>";
const botId = "<update with your bot ID>";
const revisionId = "@latest";
const authToken = "Bearer <update with your token>";

/**
 * These values can also be changed based on the user using the bot and the name of the connector.
 */
const userId = "<userid>";
const userName = "<as per choice>";
const connectorName = "<Web site / Main page / as per choice>";